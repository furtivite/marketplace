# === Logs ===
# папка логов
/logs/
/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# === Dependency directories ===
/node_modules/

# === Build outputs ===
/dist/
/dist-ssr/
/build/
/storybook-static/

# === Coverage reports ===
/coverage/

# === Environment files ===
# явно игнорируем все .env-файлы
.env
.env.*
*.local        # у вас уже есть, но на всякий случай
!*.local.example  # (если у вас есть шаблоны .local.example)

# === Editor & OS temp ===
.vscode/*
!.vscode/extensions.json
.idea/
/.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# === Cache, temp & tool files ===
/.cache/
/.turbo/
storybook.log
*.tmp
*.temp

# === Other unwanted ===
# сгенерированные lock-файлы пакетов (не рекомендую, но если у вас монорепо)
/npm-debug.log*
/yarn-debug.log*
# если используете pnpm, но не хотите коммитить локальные настройки
.pnpm/
